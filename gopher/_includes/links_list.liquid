{%- for entry in include.entries -%}
1{% if entry.title %}{{entry.title}}{% elsif entry.hostname %}{{entry.hostname}}{% else %}{{entry.selector}}{% endif %}	{{entry.selector}}{% if entry.hostname %}	{{entry.hostname}}{% endif %}{% if entry.port %}	{{entry.port}}{% endif %}
{%- endfor -%}