---
layout: null
---
{%- assign lines = content | split_by_newline -%}

{%- for line in lines -%}
	{{ line | prepend: 'i' | append: '	/'}}
{% endfor %}
iAtom Feed	/feed.xml

{% include post_list.liquid -%}