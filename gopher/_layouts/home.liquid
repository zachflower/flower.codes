---
layout: null
---
{%- assign lines = content | split_by_newline -%}

{%- for line in lines -%}
	{{ line | prepend: 'i' | append: '	/'}}
{% endfor -%}

--------------------------------------------------------------------------------

i# Posts	/

{% for post in site.posts  -%}
0{{post.date | date: "%Y.%m.%d"}} :: {{post.title}}	{{post.url}}
{%- endfor -%}